{
    "name": "vue3-vuetify3-default",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "clean": "rm -rf dist && rm -rf node_modules/.cache/gh-pages",
        "predeploy": "npm run clean",
        "deploy": "npm run build && gh-pages -d dist"
    },
    "dependencies": {
        "@mdi/font": "^7.1.96",
        "gh-pages": "^6.3.0",
        "vue": "^3.2.47",
        "vue-router": "^4.1.6",
        "vuetify": "^3.1.0"
    },
    "devDependencies": {
        "@vitejs/plugin-vue": "^4.0.0",
        "sass": "^1.62.0",
        "vite": "^4.2.1"
    }
}
